import withStyles from "@material-ui/core/styles/withStyles";
import React from "react";

const styles = {
  root: {}
};

function SortEvents() {
  return (
    <div>
      Sort Events
    </div>
  );
}

export default withStyles(styles)(SortEvents)